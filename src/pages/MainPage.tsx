import { Control } from '../components';

const MainPage = (): JSX.Element => {
  return (
    <main className="main-page__wrapper flex grow flex-col items-center gap-20  px-5 py-10">
      <Control />
    </main>
  );
};

export default MainPage;
