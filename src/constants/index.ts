export const LS_ABYSSALS_KEY = 'abyssals';

export const DELIMITER = '--------------------------';

export const NULL_ROOM_STATE = {
  start: null,
  end: null,
  data: null,
};

export enum Path {
  mainPage = '/',
  any = '*',
}
